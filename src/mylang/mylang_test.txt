
test1 = |a, b, c| {
	test2 = || { return a + a * b + b; };
	return test2() + || { return (a + a) * (b + b); }();
};

test33 = |a, b, c| {
	result = test1(a, b, c);
	std.print(result);
};

test33(2, 3, 4);

